spring:
  datasource:
    url: jdbc:postgresql://db.dsbmlwphwfrugmfzfadi.supabase.co:5432/postgres?sslmode=require&prepareThreshold=0
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: masjid3871postgress
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
      keepalive-time: 30000
      connection-test-query: SELECT 1

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

logging:
  level:
    com.zaxxer.hikari: DEBUG

jwt:
  expiration: 43200
  secret: 23234234
